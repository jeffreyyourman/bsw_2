import * as React from "react";
import NflDfsLayout from "../../../../../components/layouts/NflDfsLayout";
// import { SignedIn, SignedOut, UserButton, useClerk, useAuth } from "@clerk/clerk-react";

const ProjectionsPage = () => {
  // const clerk = useClerk();
  // const { getToken, isLoaded, isSignedIn , signOut} = useAuth();
  // // console.log('getToken',getToken())
  // console.log('isLoaded',isLoaded)
  // console.log('isSignedIn',isSignedIn)
  return (
    <NflDfsLayout>
      <h1>Projections Page - Coming soon</h1>
      {/* <button className="sign-up-btn" onClick={() => clerk.openSignUp({})}>
        Sign up
      </button>

      <button className="sign-up-btn" onClick={() => signOut()}>
        Sign out
      </button> */}
    </NflDfsLayout>
  )
};

export default ProjectionsPage;
